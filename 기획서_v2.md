# md-to-ppt v2 기획서

## 배경 및 문제 정의

현재 파이프라인은 **파서(Parser)** 로 동작 중.  
MD 파일을 그대로 불릿으로 옮기는 수준이라 발표 자료로 쓰기 어려움.

**목표**: LLM을 **프레젠테이션 컨설턴트**로 동작하게 전환.  
**벤치마크**: 젠스파크(Genspark) AI 슬라이드 수준의 내러티브·디자인 품질.

---

## 주요 사용 맥락

- **입력 문서**: 시스템 설명 문서, 유지보수 기획안 등
- **사용 목적**: 개인 아카이브 또는 사내 발표
- **청중**: 개발팀 / 기획팀 / 경영진 (발표마다 다름)

---

## 기능 요구사항 (FR)

### FR1. 입력 필드 추가 — 발표 목적 & 청중

```
발표 목적: [예: 신규 팀원에게 시스템 구조를 설명하는 발표]
청중:     [예: 개발팀 / 기획팀 / C레벨 경영진]
```

- LLM 프롬프트에 전달해 전체 톤·깊이·용어 수준 결정
- placeholder로 입력 예시 제공

### FR2. LLM 내용 재구성 (프롬프트 전면 개편)

- 불릿 복붙 금지 → 발표 목적에 맞게 **요약·재작성**
- 슬라이드당 불릿 최대 3개, 한 줄씩 핵심만
- 불릿에 관련 이모지 자동 삽입 (아이콘 대체, 예: `⚡ 처리 속도 2x 향상`)
- **최대 8장** (품질 우선)

### FR3. 내러티브 구조 강제

LLM이 반드시 이 흐름으로 슬라이드 구성:

```
표지(title) → 목차(agenda) → [문제/배경] → [본론 2~4장] → [인사이트/분석] → 결론(summary)
```

### FR4. 슬라이드 타입 확장

| 타입 | 용도 | 상태 |
|------|------|------|
| `title` | 표지 | 기존 |
| `agenda` | 목차 | **신규** |
| `section` | 챕터 구분 | **신규** — ghost text 배경으로 시각적 전환점 |
| `content` | 불릿 목록 | 기존 (불릿 ≤ 3) |
| `stat` | 숫자/지표 강조 | **신규** — 큰 숫자 카드 |
| `two_column` | 비교/대조 | **신규** — 좌우 분할 레이아웃 |
| `quote` | 핵심 메시지 강조 | **신규** — 인용 블록 |
| `chart` | 데이터 시각화 | 기존 |
| `summary` | 핵심 요약/CTA | **신규** |

### FR5. 동적 테마 시스템

기존 "디자인 힌트" 필드를 **테마 설명** 필드로 확장.

**입력 예시:**
- `"깔끔한 화이트, 포인트는 딥블루"`
- `"따뜻한 베이지 톤, 골드 강조색, 고급스러운 느낌"`
- `"다크 네이비 유지하되 퍼플 포인트"`
- `"미니멀 라이트그레이, 민트 강조"`

**동작 방식:**  
LLM이 슬라이드 JSON 안에 `theme` 블록 함께 반환 →  
`pptx_builder`가 하드코딩 색상 대신 `theme` 값으로 렌더링.

```json
{
  "theme": {
    "bg": "#FAFAFA",
    "surface": "#F0F0F0",
    "accent": "#1A3A6B",
    "text": "#1A1A1A",
    "subtext": "#666666",
    "body": "#333333"
  },
  "title": "...",
  "slides": [...]
}
```

---

## 비기능 요구사항 (NFR)

- **NFR1**: 외부 이미지/아이콘 API 불필요 — 이모지 + python-pptx 도형만 사용
- **NFR2**: 발표자 노트(Speaker Notes) 미지원
- **NFR3**: 인접한 슬라이드는 시각적으로 구분 가능해야 함
- **NFR4**: 다크/라이트 양쪽 테마 모두 정상 동작

---

## 구현 범위 요약

```
[입력 UI 변경]
  + 발표 목적 필드 (placeholder 포함)
  + 청중 필드 (placeholder 포함)
  ~ 디자인 힌트 → 테마 설명으로 확장

[pipeline/structurer.py]
  + 발표 목적/청중을 시스템 프롬프트에 주입
  + 내러티브 구조 강제 (표지→목차→본론→결론)
  + 슬라이드당 불릿 ≤ 3, 요약/재작성 지시
  + 불릿 이모지 포함 지시
  + theme 블록 JSON 출력 지시
  + 최대 8장 제한

[pipeline/pptx_builder.py]
  + agenda   : 번호 목록 목차 슬라이드
  + section  : 챕터 구분 (ghost text 배경 + 강조 바)
  + stat     : 큰 숫자/지표 카드 레이아웃
  + two_column: 좌우 분할 비교 레이아웃
  + quote    : 인용 강조 블록 (큰 따옴표 도형)
  + summary  : 이모지 체크리스트형 결론 슬라이드
  ~ 모든 색상을 theme 객체에서 동적으로 읽기

[static/index.html]
  + 발표 목적 입력 필드
  + 청중 입력 필드
  ~ 디자인 힌트 → 테마 설명 레이블/placeholder 변경
```
